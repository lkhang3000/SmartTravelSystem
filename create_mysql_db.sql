-- Tạo database cho SmartTravel
CREATE DATABASE IF NOT EXISTS smarttravel_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Nếu cần tạo user mới (tùy chọn)
-- CREATE USER 'smarttravel_user'@'localhost' IDENTIFIED BY 'your_password';
-- GRANT ALL PRIVILEGES ON smarttravel_db.* TO 'smarttravel_user'@'localhost';
-- FLUSH PRIVILEGES;
